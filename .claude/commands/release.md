---
description: 출시 전 종합 검증 — 정적 분석·테스트·에이전트 4종 병렬 검토 후 배포 가능 여부 판정
---

앱스토어/플레이스토어 제출 전 최종 게이트. 순서대로 실행하며 중간에 실패하면 즉시 중단한다.

**1단계 — 정적 분석**
```
flutter analyze
```
경고가 1개라도 있으면 이후 단계로 넘어가지 않는다. 수정 후 재실행 요청.

**2단계 — 테스트**
```
flutter test
```
실패한 테스트가 있으면 중단하고 원인을 설명한다.
테스트가 없으면 "테스트 없음 — 수동 검증 필요" 로 표시하고 계속 진행한다.

**3단계 — 병렬 에이전트 검증**
아래 에이전트를 병렬로 실행한다. `git diff main..HEAD` 범위의 변경사항을 기준으로 검토.

- `perf-reviewer` 에이전트: rebuild, jank, 메모리 이슈
- `logic-reviewer` 에이전트: null safety, async 경합, 상태 전이, 에러 처리
- `side-effect-checker` 에이전트: API 중복 호출, 로컬 저장, FCM
- `permission-auditor` 에이전트: 화면 접근 제어, 토큰·세션, 라우팅

**4단계 — 종합 판정**
아래 형식으로 최종 보고:

```
## 출시 전 종합 검증 결과

### 정적 분석
✅ 경고 0개 / ❌ N개 (목록)

### 테스트
✅ N개 통과 / ❌ 실패 (목록) / ⚠️ 테스트 없음

### 에이전트 검증 요약
🔴 즉시 수정 필요: N건
🟠 출시 전 수정 권장: N건
🟡 주의: N건

### 세부 항목
[check 명령과 동일한 위험도별 목록]

---
## 최종 판정

🟢 배포 가능
또는
🔴 배포 불가 — [이유]
```

**배포 불가 기준**: 🔴 항목이 1개 이상이거나, 정적 분석 경고가 있거나, 테스트 실패가 있으면 배포 불가.
